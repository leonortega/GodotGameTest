# Audio Folder

Place imported music and sound effects here.

The runtime already checks these locations:

- `res://audio/music/`
- `res://audio/sfx/`

If the expected files are present, the game will use them automatically. If not, it falls back to generated placeholder audio.
